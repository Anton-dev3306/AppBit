spring:
  application:
    name: appbit

  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: ${PORT:8080}

flowise:
  url: ${FLOWISE_URL:https://cloud.flowiseai.com}
  chatflow-id: ${FLOWISE_CHATFLOW_ID}
  api-key: ${FLOWISE_API_KEY}

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

logging:
  level:
    com.appbit.matching: INFO